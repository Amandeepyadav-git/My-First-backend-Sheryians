users can write posts
login and register
logout
post creation
post like
post edit (only to the creator)
users can be created



// app.get("/test", (req, res) => {
//   res.render("test");
// });

// app.post("/upload", upload.single("image"), (req, res) => {
//   console.log("req.file= ", req.file);
//   res.send(req.file);
// });